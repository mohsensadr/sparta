#pragma once

#include <unordered_map>
#include <memory>

namespace std {
    namespace tr1 {
        using std::unordered_map;
        using std::shared_ptr;
        using std::weak_ptr;
        using std::unique_ptr;
    }
}

